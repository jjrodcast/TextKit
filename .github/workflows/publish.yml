name: Publish to Maven Central

on:
  # Publish when a GitHub Release is published from the Releases page
  release:
    types: [published]
  # Allow manual runs from the Actions tab
  workflow_dispatch:
    inputs:
      version:
        description: 'Versión a publicar (ej. 1.0.0-alpha01). Vacío = usar el tag seleccionado'
        required: false

jobs:
  publish:
    name: Publish :shared to Maven Central
    # macOS runner so all KMP targets (incl. iOS) can be built and published
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Resolve version
        id: version
        run: |
          V="${{ inputs.version }}"
          echo "value=${V:-${GITHUB_REF_NAME#v}}" >> "$GITHUB_OUTPUT"

      # SIGNING_KEY is stored base64-encoded to avoid newline mangling in secrets.
      # Generate it with: gpg --export-secret-keys --armor <KEY_ID> | base64
      - name: Decode signing key
        run: |
          {
            echo "SIGNING_KEY_ARMORED<<__GPG_EOF__"
            echo "${{ secrets.SIGNING_KEY }}" | base64 --decode
            echo "__GPG_EOF__"
          } >> "$GITHUB_ENV"

      - name: Publish to Maven Central
        run: ./gradlew :shared:publishToMavenCentral -PlibVersion=${{ steps.version.outputs.value }} --no-configuration-cache --stacktrace
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
          ORG_GRADLE_PROJECT_signingInMemoryKey: ${{ env.SIGNING_KEY_ARMORED }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyPassword: ${{ secrets.SIGNING_PASSWORD }}
